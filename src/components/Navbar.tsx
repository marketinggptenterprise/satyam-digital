"use client"; import { useState } from 'react'; import { Link, useNavigate, useSearchParams } from 'react-router-dom'; import { Settings, Search, MapPin, ShoppingCart, Menu } from 'lucide-react'; import { Button } from './ui/button'; import { Input } from './ui/input'; import { CartDrawer } from './CartDrawer'; import { useCart } from '../hooks/useCart'; export const Navbar = () => { const { totalItems } = useCart(); const navigate = useNavigate(); const [searchParams] = useSearchParams(); const [searchQuery, setSearchQuery] = useState(searchParams.get('q') || ''); const handleSearch = (e: React.FormEvent) => { e.preventDefault(); if (searchQuery.trim()) { navigate(`/?q=${encodeURIComponent(searchQuery.trim())}`); } else { navigate('/'); } }; return ( <header className="w-full bg-white shadow-sm sticky top-0 z-50"> {/* Top Bar */} <div className="bg-primary text-white py-2 text-xs"> <div className="container flex justify-between items-center"> <div className="flex items-center gap-4"> <span className="flex items-center gap-1"><MapPin className="h-3 w-3" /> Select Location</span> <span>Contact: +91 9932026227</span> </div> <div className="flex items-center gap-4"> <Link to="/admin" className="hover:underline flex items-center gap-1"> <Settings className="h-3 w-3" /> Admin Panel </Link> </div> </div> </div> {/* Main Header */} <div className="container py-4 flex items-center gap-8"> <Link to="/" className="flex items-center shrink-0"> <img src="/logo.png" alt="Satyam Digital" className="h-16 w-auto object-contain" /> <div className="ml-2 leading-tight hidden lg:block"> <span className="text-xl font-black text-primary block">SATYAM</span> <span className="text-sm font-bold text-secondary tracking-widest">DIGITAL</span> </div> </Link> <form onSubmit={handleSearch} className="flex-1 relative hidden md:block"> <Input placeholder="Search for Mobiles, Accessories, TV & more..." className="w-full pl-4 pr-12 h-11 border-2 border-primary/20 focus-visible:border-primary rounded-full" value={searchQuery} onChange={(e) => setSearchQuery(e.target.value)} /> <Button type="submit" size="icon" className="absolute right-1 top-1 h-9 w-9 rounded-full bg-primary hover:bg-primary/90"> <Search className="h-4 w-4 text-white" /> </Button> </form> <div className="flex items-center gap-6 shrink-0"> <CartDrawer /> </div> </div> {/* Category Bar */} <div className="border-t bg-white hidden lg:block"> <div className="container flex items-center gap-8 py-2"> <Button variant="ghost" className="font-bold text-primary gap-2"> <Menu className="h-4 w-4" /> Shop By Category </Button> <nav className="flex items-center gap-6 text-sm font-medium text-gray-700"> <Link to="/?cat=mobiles" className="hover:text-primary">Mobiles</Link> <Link to="/?cat=laptops" className="hover:text-primary">Laptops</Link> <Link to="/?cat=electronics" className="hover:text-primary">Smart TV</Link> <Link to="/?cat=appliances" className="hover:text-primary">Appliances</Link> <Link to="/?cat=audio" className="hover:text-primary">Accessories</Link> <Link to="/?q=offer" className="text-red-600 font-bold">Offers</Link> </nav> </div> </div> </header> ); };